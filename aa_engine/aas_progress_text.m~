function aas_progress_text(loops

percentDone = 100 * ind / length(fns);
msg = sprintf('Percent done copying files: %3.2f', percentDone);
fprintf([reverseStr, msg]);
reverseStr = repmat(sprintf('\b'), 1, length(msg));
if(percentDone == 100)
    fprintf('\n');
end